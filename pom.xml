<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<artifactId>fess-ds-trello</artifactId>
	<packaging>jar</packaging>
	<name>Trello Data Store</name>
	<!-- Independent semver, not tied to the Fess version below (parent's own
	     <version> is what actually needs to track Fess, since that's the real
	     build-time compatibility declaration; this plugin's own version has
	     no such constraint: it's not published to maven.codelibs.org, so
	     nothing resolves it by matching Fess's version the way FESS_PLUGINS
	     does for the official codelibs fess-ds-* plugins. Bump this on its
	     own release cadence; bump the parent version separately, only when
	     actually rebuilding against a newer Fess. -->
	<version>1.3.0</version>
	<scm>
		<connection>scm:git:git@github.com:ram-electronic/fess-ds-trello.git</connection>
		<developerConnection>scm:git:git@github.com:ram-electronic/fess-ds-trello.git</developerConnection>
		<url>https://github.com/ram-electronic/fess-ds-trello</url>
	</scm>
	<parent>
		<groupId>org.codelibs.fess</groupId>
		<artifactId>fess-parent</artifactId>
		<version>15.8.0</version>
		<relativePath />
	</parent>
	<build>
		<plugins>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
			</plugin>
			<plugin>
				<artifactId>maven-source-plugin</artifactId>
			</plugin>
			<plugin>
				<artifactId>maven-javadoc-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>com.mycila</groupId>
				<artifactId>license-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>net.revelc.code.formatter</groupId>
				<artifactId>formatter-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
	<!-- This plugin is not published to Maven Central or maven.codelibs.org,
	     so unlike codelibs' own fess-ds-* plugins there is no distributionManagement
	     here for `mvn deploy` to target. Build a jar with `mvn package` and install
	     it into the Fess container manually (see README) instead of relying on
	     FESS_PLUGINS-style auto-fetch. -->
	<dependencies>
		<dependency>
			<groupId>jakarta.annotation</groupId>
			<artifactId>jakarta.annotation-api</artifactId>
			<version>${jakarta.annotation.api.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>jakarta.servlet</groupId>
			<artifactId>jakarta.servlet-api</artifactId>
			<version>${jakarta.servlet.api.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.codelibs</groupId>
			<artifactId>corelib</artifactId>
			<version>${corelib.version}</version>
		</dependency>
		<dependency>
			<groupId>org.lastaflute</groupId>
			<artifactId>lastaflute</artifactId>
			<version>${lastaflute.version}</version>
		</dependency>
		<dependency>
			<groupId>org.dbflute</groupId>
			<artifactId>dbflute-runtime</artifactId>
			<version>${dbflute.version}</version>
		</dependency>
		<dependency>
			<groupId>org.codelibs.fess</groupId>
			<artifactId>fess</artifactId>
			<version>${fess.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.opensearch</groupId>
			<artifactId>opensearch</artifactId>
			<version>${opensearch.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>${slf4j.version}</version>
		</dependency>
		<!-- Renders Trello's Markdown-formatted card/comment text to plain text
		     for Fess's content/digest fields (see MarkdownPlainTextRenderer),
		     already pulled in transitively at provided scope via fess-parent's
		     own dependencyManagement, declared explicitly here (like fess/
		     opensearch above) rather than relying on that silently. -->
		<dependency>
			<groupId>org.commonmark</groupId>
			<artifactId>commonmark</artifactId>
			<!-- Not managed by fess-parent's own dependencyManagement (it's only
			     pulled in transitively via another dependency's own POM), so
			     pinned explicitly here, matching the version already resolved
			     transitively (confirmed via mvn dependency:tree). -->
			<version>0.24.0</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>${junit.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.dbflute.utflute</groupId>
			<artifactId>utflute-lastaflute</artifactId>
			<version>${utflute.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>
</project>
